{
  "name": "bmad-test",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm --workspaces run test",
    "build": "npm --workspaces run build",
    "lint": "npm --workspaces run lint",
    "e2e": "npm --workspace packages/frontend run e2e",
    "ci": "npm run lint && npm test && npm run e2e"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
